package controls;

public class CustomController extends Controller {

    /**
     * Controller where you can rebind controls easily
     * all the keys are custom bound
     */
    public CustomController(int left, int right, int up, int down) {
        super(left, right, up, down);
    }
}
